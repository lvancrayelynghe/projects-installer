---

- name: Install packages
  composer:
    working_dir: "{{ item.path }}"
    command: "install"
  with_items: items.results | map(attribute='ansible_facts') | byattr("state", "present") | selectattr("composer", "defined") | list
  when: item.composer != false
